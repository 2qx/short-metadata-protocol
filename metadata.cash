pragma cashscript ^0.10.0;


contract metadata() {
    function test01() {
        

        bytes output = new LockingBytecodeNullData([
            0x04 + 0x534D5030,
            0x1201,
            0x04 + bytes('tBCH'),
            bytes(8) + bytes('test BCH')
        ]);
        require(tx.outputs[0].lockingBytecode == output);
    }
}

